PREFIX : @schemaUri
CONSTRUCT {
    ?formalBody
        a :FormalBody ;
        :formalBodyName ?formalBodyName .
}
WHERE {
    BIND(@formal_body_id AS ?formalBody)
    ?formalBody
        a :FormalBody ;
        :formalBodyName ?formalBodyName .
}
